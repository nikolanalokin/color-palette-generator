import styled from '@emotion/styled'

export const BaseSelect = styled.select({
    margin: 0,
    border: 0,
    paddingInline: '16px',
    height: '48px',
    borderRadius: '8px',
    backgroundColor: 'rgba(0 0 0 / .1)',
})
